require "spec_helper"

RSpec.describe Pronto::Clippy do
  it "has a version number" do
    expect(Pronto::Clippy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
